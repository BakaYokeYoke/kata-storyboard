{
  "name": "kata-storyboard",
  "private": true,
  "description": "Kanban de Visions et storyboards Toyota Kata",
  "scripts": {
    "serve": "python3 -m http.server 8766 --bind 127.0.0.1",
    "test": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.50.0"
  }
}
